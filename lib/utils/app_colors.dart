import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color.fromARGB(255, 17, 16, 58);
  static Color accentColor =
      const Color.fromARGB(255, 27, 28, 72).withOpacity(0.8);
  static const Color selectedColor = Color.fromARGB(255, 254, 207, 88);
  static const Color white = Colors.white;
  static const Color transparent = Colors.transparent;
  static const Color red = Colors.red;
}
